package com.familyexpensetracker.config;

// Empty configuration class if additional JWT beans are required, 
// though most JWT configuration is handled by JwtTokenProvider.
// Maintained for project structure consistency as requested.
import org.springframework.context.annotation.Configuration;

@Configuration
public class JwtConfig {
}
